import { Navigation } from 'components/Navigation/Navigation';
import { HeaderWrapper } from './Header.styled';

export const Header = () => (
  <HeaderWrapper>
    <Navigation />
  </HeaderWrapper>
);
